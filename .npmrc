auto-install-peers=true
strict-peer-dependencies=false
# Next.js + NestJS both behave well with pnpm's isolated node_modules.
# Keep the store strict; opt specific packages into hoisting only if needed.
