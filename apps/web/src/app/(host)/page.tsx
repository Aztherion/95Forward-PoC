import { PagePlaceholder } from "@/components/shell";

export default function HomePage() {
  return (
    <PagePlaceholder
      eyebrow="Keystone CRM"
      title="Home"
      subtitle="Water For People"
      empty="Built in a later initiative. Your overview lands here once the workspace grows."
    />
  );
}
