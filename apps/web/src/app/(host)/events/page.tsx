import { PagePlaceholder } from "@/components/shell";

export default function EventsPage() {
  return (
    <PagePlaceholder
      eyebrow="Keystone CRM"
      title="Events"
      empty="Built in a later initiative. Event planning and attendance show up here."
    />
  );
}
