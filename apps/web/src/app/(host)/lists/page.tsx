import { PagePlaceholder } from "@/components/shell";

export default function ListsPage() {
  return (
    <PagePlaceholder
      eyebrow="Keystone CRM"
      title="Lists"
      empty="Built in a later initiative. Saved lists and segments will live here."
    />
  );
}
