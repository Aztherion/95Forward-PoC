import { PagePlaceholder } from "@/components/shell";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      eyebrow="Keystone CRM"
      title="Settings"
      empty="Built in a later initiative. Preferences and configuration arrive here later."
    />
  );
}
