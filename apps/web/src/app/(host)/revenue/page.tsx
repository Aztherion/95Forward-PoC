import { PagePlaceholder } from "@/components/shell";

export default function RevenuePage() {
  return (
    <PagePlaceholder
      eyebrow="Keystone CRM"
      title="Revenue"
      empty="Built in a later initiative. Gift and revenue records arrive here later."
    />
  );
}
