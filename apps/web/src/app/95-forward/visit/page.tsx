import { PagePlaceholder } from "@/components/shell";

export default function VisitPage() {
  return (
    <PagePlaceholder
      eyebrow="95 Forward"
      title="Visit mode"
      subtitle="Present at your side, never dominating"
      empty="Built in a later initiative. The live-visit companion will open here."
    />
  );
}
