import { PagePlaceholder } from "@/components/shell";

export const dynamic = "force-dynamic";

export default function ForecastPage() {
  return (
    <PagePlaceholder
      eyebrow="95 Forward · the forecast room"
      title="Forecast"
      subtitle="95 Forward"
      empty="Best, most likely and worst, off ten thousand simulated years — and the names behind each. Being built in I27; the simulation behind it is already live."
    />
  );
}
