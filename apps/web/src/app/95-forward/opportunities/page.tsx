import { PagePlaceholder } from "@/components/shell";

export const dynamic = "force-dynamic";

export default function OpportunitiesPage() {
  return (
    <PagePlaceholder
      eyebrow="95 Forward · the pipeline"
      title="Opportunities"
      subtitle="95 Forward"
      empty="Every live opportunity in one grid, by stage and by initiative. Being built in I29; the opportunity model behind it is already live."
    />
  );
}
