<!--
  Claude Code reads CLAUDE.md; OmO and other agents read AGENTS.md. The guidance lives in
  AGENTS.md and is imported here so both read the same file instead of drifting apart.
  Claude Code expands @-imports into context at launch — AGENTS.md is NOT read natively, so
  this import is load-bearing, not decorative. Do not delete it.
-->

@AGENTS.md
