# DigitalOcean App Platform spec for 95 Forward.
# Operator setup before `doctl apps create --spec .do/app.yaml`:
#   - the database `cluster_name` is set to the existing vector-enabled managed PG cluster
#     (the spec attaches that cluster; it does not provision a throwaway dev database)
#   - set instance sizes as desired
#   - ensure the managed PG vector extension is allowed (the PRE_DEPLOY migrate
#     job runs CREATE EXTENSION IF NOT EXISTS vector)
#   - supply real values for every REPLACE_ME (Auth0 client id/secret; the
#     AUTH0_SECRET is a 32-byte hex from `openssl rand -hex 32`)
# The worker is a background `workers:` component (non-routable): the web app enqueues jobs via the
# database, not via HTTP, so the worker serves no public traffic and declares no port/route. It runs
# with its own minimal env (getWorkerEnv): no Auth0 vars; it needs APP_DATABASE_URL for RLS-scoped
# job handlers (its NestJS server binds internally on WORKER_PORT).
#
# Demo AI config: AI_MODE=mock + EMBEDDING_MODE=live, with RESEARCH_MODE=demo keeping OSINT on
# seeded data only (the responsible-AI guardrail forbids live research on real individuals).
#
# D1 flipped AI_MODE from live to mock. I28's fixture drafts are composed FROM the record in front
# of them, so they are what you want on screen anyway — and they are instant, deterministic, and
# cannot invent a fact or a person mid-demo. A live call adds a few seconds of visible wait and a
# class of failure nobody wants in front of a stakeholder. EMBEDDING_MODE stays live because
# retrieval quality is real work the demo shows off; it needs OPENAI_API_KEY.
#
# DEMO_TODAY is the anchor every relative figure is computed against — "81 days silent", the
# slippage chain, the forecast's vintage date, and the deterministic simulation seed. It matches
# the seed's own anchor. Unset, the app falls back to the compiled-in constant; a malformed value
# fails at boot rather than silently reverting to wall time.
#
# ALLOW_DESTRUCTIVE_RESET=true (app-level) lets the operator run the guarded truncate-and-reseed
# against THIS demo database from a Console shell (see docs/deployment-and-ops-runbook.md). It is
# safe here only because the reset still refuses unless RESEARCH_MODE!=live AND --confirm is passed;
# never set it on a database holding real data.
name: ninety-five-forward
region: ams

envs:
  - key: ALLOW_DESTRUCTIVE_RESET
    value: "true"
  # The fourth reset guard (D1). The other three establish that SOME database may be reset and say
  # nothing about WHICH — an operator with a Console shell here and a DATABASE_URL from elsewhere
  # exported in their own shell satisfies all three. Set this to the demo database's name and the
  # reset refuses to run anywhere else. It must match the database in ${forward-db.DATABASE_URL}.
  - key: DEMO_DATABASE_NAME
    value: REPLACE_ME_WITH_THE_DEMO_DB_NAME

databases:
  - name: forward-db # keep this logical name — bindings use ${forward-db.DATABASE_URL}
    engine: PG
    cluster_name: db-pgsql-95fwd-poc # the managed PG cluster (region ams3)
    production: true

jobs:
  - name: migrate
    kind: PRE_DEPLOY
    github:
      repo: Aztherion/95Forward-PoC
      branch: main
    source_dir: /
    instance_size_slug: basic-xxs
    build_command: corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm install --frozen-lockfile && pnpm --filter @95forward/db build
    run_command: pnpm --filter @95forward/db migrate
    envs:
      # The buildpack prunes devDependencies (tsc/tsx) when NODE_ENV=production; this BUILD_TIME flag
      # skips that prune so the build (and the migrate job's tsx runtime) keep their toolchain.
      - key: PNPM_SKIP_PRUNING
        scope: BUILD_TIME
        value: "true"
      - key: DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        value: ${forward-db.DATABASE_URL}

services:
  - name: web
    github:
      repo: Aztherion/95Forward-PoC
      branch: main
      deploy_on_push: true
    source_dir: /
    instance_size_slug: basic-xs
    instance_count: 1
    http_port: 8080
    health_check:
      http_path: /
    build_command: corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm install --frozen-lockfile && export NEXT_PUBLIC_BUILD_SHA=$(git rev-parse --short HEAD 2>/dev/null || echo unknown) && pnpm --filter @95forward/shared build && pnpm --filter @95forward/db build && pnpm --filter @95forward/ai build && pnpm --filter @95forward/web build
    run_command: pnpm --filter @95forward/web exec next start --hostname 0.0.0.0 --port ${PORT}
    envs:
      # BUILD_TIME-only: keep devDependencies (tsc) for the build; runtime stays NODE_ENV=production.
      - key: PNPM_SKIP_PRUNING
        scope: BUILD_TIME
        value: "true"
      - key: NODE_ENV
        value: production
      - key: APP_ENV
        value: production
      - key: DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        value: ${forward-db.DATABASE_URL}
      - key: APP_DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME
      - key: NEXT_PUBLIC_APP_NAME
        value: "95 Forward"
      - key: NEXT_PUBLIC_HOST_NAME
        value: "Keystone CRM"
      - key: NEXT_PUBLIC_HOST_ORG
        value: "Water For People"
      - key: AUTH0_DOMAIN
        scope: RUN_AND_BUILD_TIME
        value: your-tenant.us.auth0.com
      - key: AUTH0_CLIENT_ID
        scope: RUN_AND_BUILD_TIME
        value: REPLACE_ME
      - key: AUTH0_CLIENT_SECRET
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME
      - key: AUTH0_SECRET
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME
      - key: APP_BASE_URL
        scope: RUN_AND_BUILD_TIME
        value: ${APP_URL}
      # The demo's "today". Must match the seed's anchor or every relative figure drifts — see the
      # header note. Same value on web and worker.
      - key: DEMO_TODAY
        value: "2026-09-12"
      - key: AI_MODE
        value: mock
      - key: EMBEDDING_MODE
        value: live
      - key: RESEARCH_MODE
        value: demo
      - key: ANTHROPIC_API_KEY
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME
      - key: OPENAI_API_KEY
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME
      # In-app feedback → GitHub issue (Initiative 15). FEEDBACK_GITHUB_TOKEN is a DEDICATED
      # fine-grained PAT scoped to THIS repo, Issues: Read & Write only — distinct from any CI
      # token (least privilege for a user-facing path). Server-only; never reaches the browser.
      - key: FEEDBACK_MODE
        value: live
      - key: FEEDBACK_REPO
        value: Aztherion/95Forward-PoC
      - key: FEEDBACK_GITHUB_TOKEN
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME

workers:
  - name: worker
    github:
      repo: Aztherion/95Forward-PoC
      branch: main
      deploy_on_push: true
    source_dir: /
    instance_size_slug: basic-xxs
    instance_count: 1
    build_command: corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm install --frozen-lockfile && pnpm --filter @95forward/shared build && pnpm --filter @95forward/db build && pnpm --filter @95forward/ai build && pnpm --filter @95forward/worker build
    run_command: pnpm --filter @95forward/worker start
    envs:
      # BUILD_TIME-only: keep devDependencies (tsc) for the build; runtime stays NODE_ENV=production.
      - key: PNPM_SKIP_PRUNING
        scope: BUILD_TIME
        value: "true"
      - key: NODE_ENV
        value: production
      - key: APP_ENV
        value: production
      - key: WORKER_PORT
        value: "8080"
      - key: DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        value: ${forward-db.DATABASE_URL}
      - key: APP_DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME
      - key: JOBS_CONCURRENCY
        value: "3"
      - key: JOBS_SCHEMA
        value: graphile_worker
      - key: DEMO_TODAY
        value: "2026-09-12"
      - key: AI_MODE
        value: mock
      - key: EMBEDDING_MODE
        value: live
      - key: RESEARCH_MODE
        value: demo
      - key: ANTHROPIC_API_KEY
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME
      - key: OPENAI_API_KEY
        scope: RUN_AND_BUILD_TIME
        type: SECRET
        value: REPLACE_ME
